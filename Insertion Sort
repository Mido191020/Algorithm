void insertion_sort(){
    int n=8;
    int a[]={9,2,10,0,5,3,90,85};
    for (int i = n-1; i>=0 ; i--) {
        for (int j = i-1; j >=0 ; j--) {
            if (a[i]<a[j]){
                swap(a[i],a[j]);
            }
        }
    }
    for(auto it:a){
        cout<<it<<" ";
    }
}
